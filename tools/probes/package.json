{
  "name": "@tab/probes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "x402": "node --experimental-strip-types --env-file=../../.env src/x402-loop.ts",
    "typecheck": "tsc --noEmit",
    "probe:x402": "node --experimental-strip-types --env-file=../../.env src/x402-loop.ts",
    "probe:x402:hts": "node --experimental-strip-types --env-file=../../.env src/x402-loop.ts --hts",
    "probe:latency": "node --experimental-strip-types --env-file=../../.env src/redis-latency.ts",
    "probe:schedule": "node --experimental-strip-types --env-file=../../.env src/schedule-tick.ts",
    "probe:adapter": "node --experimental-strip-types --env-file=../../.env src/x402-adapter.ts",
    "probe:adapter:hts": "node --experimental-strip-types --env-file=../../.env src/x402-adapter.ts --hts",
    "probe:protocol": "node --experimental-strip-types --env-file=../../.env src/protocol-roundtrip.ts"
  },
  "dependencies": {
    "@hiero-ledger/sdk": "catalog:",
    "@tab/mirror": "workspace:*",
    "@tab/money": "workspace:*",
    "@x402/core": "catalog:",
    "@x402/fetch": "catalog:",
    "@x402/hedera": "catalog:",
    "express": "^5.1.0",
    "@x402/express": "catalog:",
    "ioredis": "catalog:",
    "@tab/hedera": "workspace:*",
    "@tab/x402": "workspace:*",
    "postgres": "^3.4.5",
    "@tab/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0"
  }
}
