{
  "name": "@tab/verify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "verify-tab": "node --experimental-strip-types --env-file=../../.env src/verify-tab.ts",
    "verify-ceiling": "node --experimental-strip-types --env-file=../../.env src/verify-ceiling.ts",
    "typecheck": "tsc --noEmit",
    "test:unit": "node --experimental-strip-types --test src/*.test.ts"
  },
  "dependencies": {
    "@tab/graph": "workspace:*",
    "@tab/ledger": "workspace:*",
    "@tab/mirror": "workspace:*",
    "@tab/money": "workspace:*",
    "@tab/params": "workspace:*",
    "@tab/protocol": "workspace:*",
    "@tab/scoring": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0"
  }
}
